
struct Sensor {
    1: uint8 value;                 // The raw value of the sensor.
    2: bool activated;              // Indicates whether the sensor's value has passed the sensitivity threshold.
    3: timepoint lastActivation;    // Indicates the time when the sensor was last activated.
}
